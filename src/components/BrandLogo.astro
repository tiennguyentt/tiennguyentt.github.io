---
import { withBase } from '../lib/url';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<img
  class:list={['brand-logo', className]}
  src={withBase('/brand/tien-logo-256.png')}
  width="256"
  height="256"
  alt=""
  decoding="async"
/>
